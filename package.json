{
  "name": "findjobs",
  "version": "1.0.0",
  "description": "Place where you can apply for job or post a job.",
  "scripts": {
    "dev": "ts-node-dev ./src/index.ts",
    "start": "node ./server/dist/index.js",
    "install": "npm install --prefix client && npm install --prefix server",
    "heroku-postbuild": "npm run install && npm run build --prefix client && npm run build --prefix server"
  },
  "engines": {
    "node": "14.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
